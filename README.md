# selecao-brasileira

Base limpa com Vue 3 + Vite, pronta para desenvolvimento.

## Scripts

- `npm run dev` inicia o servidor local.
- `npm run build` gera a build de producao.
- `npm run preview` sobe a build localmente.
